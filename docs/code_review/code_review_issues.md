# Code Review Issues

Consolidated from methods documentation review. Issues are organized by section.
Issues marked ~~strikethrough~~ with **RESOLVED** have been fixed.

---

## Critical Issues

| # | Section | Issue | Status |
|---|---------|-------|--------|
| C1 | 04_human_scrnaseq | ~~DEG column schema mismatch~~ — scripts 12-13 expected Seurat-style (`avg_log2FC`, `p_val_adj`) but script 11 outputs DESeq2-style (`log2FoldChange`, `padj`) | **RESOLVED** — scripts 12-13 updated to use DESeq2 column names |
| C2 | 04_human_scrnaseq | ~~Multi-cell-type scripts load macrophage-only data~~ — scripts 15-16 loaded `macrophage_seurat.rds` (macrophage/monocyte only) instead of full annotated object | **RESOLVED** — scripts 15-16 now load `seurat_annotated.rds` (full multi-cell-type object) |
| C3 | 01_human_bulk_rnaseq | MICA sub-pipeline (5 scripts: `mica/01_prep_data.R` through `mica/05_fig5_immune.R`) contains only stub implementations with TODO comments. Functions `heatmap_median()`, `path_boxplot()`, and `cell_boxplot()` are defined but never called. No MICA output files exist in `mica/outputs/`. Any reported MICA findings are not from this pipeline. | Open |
| C4 | 01_human_bulk_rnaseq | ~~Correlation analysis uses unfiltered gene set~~ — `04_correlations.R` previously used unfiltered TPM matrix, inconsistent with `03_run_progeny.R` | **RESOLVED** — `04_correlations.R` now filters to protein-coding genes (consistent with `03_run_progeny.R`) |

---

## Warnings

| # | Section | Issue | Status |
|---|---------|-------|--------|
| W1 | 02_rat_snrnaseq | ~~Cell-level DE pseudoreplication~~ — original `FindMarkers()` cell-level tests inflate n | **RESOLVED** — converted to pseudobulk DESeq2 with per-sample aggregation |
| W2 | 05_rat_bulk_rnaseq | ~~CPM mislabeled as TPM~~ — `normalized_tpm.csv` was size-factor-normalized CPM, not true TPM | **RESOLVED** — renamed to `normalized_cpm.csv`, updated all references |
| W3 | 04_human_scrnaseq | Script 15 comment says Wilcoxon but code runs Welch t-test — `15_multicelltype_pathway.R` header (line 8) states "Wilcoxon tests" but lines 200-204 implement `t.test(..., var.equal = FALSE)`. Methods text must match the code. | Open |
| W4 | 04_human_scrnaseq | Dense matrix conversion in GSVA (~50 GB) risks OOM on standard nodes — `06_run_gsva.R` calls `as.matrix(counts_all)` at line 283; for the Xu et al. 2024 atlas (~59K genes x ~115K cells) this requires approximately 50 GB of RAM. | Open |
| W5 | 04_human_scrnaseq | ~~CellPhoneDB p-values unadjusted~~ | **RESOLVED** — BH-FDR correction now applied before filtering/display |
| W6 | 03_rat_wes | VEP cache v95 paired with VEP software v114.2 — major version mismatch. Cache version 95 (Ensembl 95, January 2019) with VEP v114.2; annotations may lack updates from later Ensembl releases. Should be documented as a limitation or cache updated. | Open |
| W7 | 03_rat_wes | SigProfilerAssignment and pybiomart not in environment.yml; separate untracked conda environment (`aging_wes`) used whose specification is not tracked in this repository. | Open |
| W8 | 03_rat_wes | BioMart live API dependency (no archive host pinned) introduces non-reproducibility — BioMart database is updated with each Ensembl release; gene symbol mappings and homolog assignments may change over time. Cache file mitigates repeated runs but initial query results are not version-locked. | Open |
| W9 | 01_human_bulk_rnaseq | ~~PROGENy reads raw TPM independently~~ — prior inconsistency where PROGENy re-read the raw TPM file separately from GSVA | **RESOLVED** — PROGENy now uses same VST matrix as GSVA |
| W10 | 01_human_bulk_rnaseq | Two different conda environments referenced across sbatch scripts — main pipeline sbatch references `erp_bulk_rnaseq`, MICA sbatch references `erp_brca_aging`; unclear whether `erp_bulk_rnaseq` is a separate environment or an error. | Open |
| W11 | 02_rat_snrnaseq | Annotation ambiguity: primary vs scType pipeline outputs diverge; unclear which feeds DE/DA — `08_differential_expression.R` and `09_differential_abundance.R` load `seurat_annotated.rds` from primary pipeline (resolution 0.4), while scType pipelines (03b/03c, resolution 1.5) produce separate output files not consumed by DE/DA. It is unclear which annotation was used for reported results. | Open |
| W12 | 02_rat_snrnaseq | Fixed 5% doublet rate not calibrated to loading density — for 10X Chromium, multiplet rate scales approximately linearly with cells loaded; a per-sample rate calibrated to loading density would be more accurate. | Open |
| W13 | 03_rat_wes | Misleading comment in oncoplot script swaps Young/Old label descriptions — `03_generate_oncoplot.py` line 217 comment states "Young (102, 107, 116) = '_Y', Old (157, 158, 167) = '_O'" but the actual code assigns `_Y` to the `YOUNG_SAMPLES` list (157, 158, 167), consistent with `02_cosmic_signatures.py`. The misleading comment could cause confusion during review or future edits. | Open |
| W14 | 03_rat_wes, 04_human_scrnaseq | Missing random seeds in Python scripts (project requirement violation) — `01_parse_vep.py`, `02_cosmic_signatures.py`, and `16_run_cellphonedb.py` do not set `np.random.seed()`. SigProfilerAssignment may use non-deterministic optimization without explicit seed control. | Open |
| W15 | 04_human_scrnaseq | Missing random seed in CellPhoneDB v5 script — `16_run_cellphonedb.py` does not set `np.random.seed()` or pass `debug_seed`. `16b_run_cellphonedb_v4.py` passes `debug_seed=42`. | Open |
| W16 | 05_rat_bulk_rnaseq | BioMart version not pinned in DESeq2 and PAM50 scripts — both `03_deseq2.R` and `04_pam50_subtyping.R` query the live Ensembl BioMart endpoint (`useMart("ensembl", ...)`) without specifying an archive host. Gene symbol mappings may differ between Ensembl releases, affecting reproducibility. | Open |
| W17 | 05_rat_bulk_rnaseq | STAR genome index provenance undocumented — index at `/ix1/alee/LO_LAB/Personal/Rahul/Reference_genome/Rat` was generated externally; STAR version used to build it is not documented. STAR indices are not portable across major versions, which could cause silent alignment errors. | Open |
| W18 | 01_human_bulk_rnaseq | Duplicate sample 135938ERPRnoNAC not explicitly excluded — per CLAUDE.md requirement, this sample is a duplicate of 1410354A13KECKHE and must be excluded from all analyses; no explicit exclusion step is present in the bulk RNA-seq pipeline. | Open |
| W19 | All | Most package versions unpinned in environment.yml; no runtime sessionInfo() captured — only `r-base=4.4.1`, `r-seurat>=5.0`, and `python=3.10` have version constraints; ComplexHeatmap, circlize, MICA, CellChat, CellPhoneDB, and GSEApy are not version-pinned or not listed at all. | Open |
| W20 | 06_spatial_biopsies | `gseapy.enrichr()` called with `organism='Human'` parameter — this parameter is deprecated/ignored in recent gseapy versions (≥1.0); organism filtering must be handled by gene set library selection instead. Results may silently include non-human gene sets if library names are not human-specific. | Open |
| W21 | 07_organoid_scrnaseq | Scripts 02, 03, 06, 07, 08 do not set explicit random seeds — `02_qc.py`, `03_preprocess.py`, `06_cell_cycle.py`, `07_single_cell_pathways.py`, and `08_inhibitor_mechanism_exploration.py` contain no `np.random.seed()`, `random.seed()`, or `random_state=` calls. Project convention for section 07 is `seed=42` / `random_state=42` (per CLAUDE.md, to preserve consistency with original analysis results). | Open |
| W22 | 01_human_bulk_rnaseq | Age group boundary definitions inconsistent between main pipeline and MICA sub-analysis — main pipeline inherits age groups directly from annotation file column `AgeRange` without validating against raw chronological age; MICA sub-analysis defines explicit cutoffs (Young: 35-45, Middle-Aged: 55-69, Elderly: ≥70). Consistency should be verified before publication. | Open |
| W23 | 01_human_bulk_rnaseq | TPM-only fallback mode saves log2(TPM) matrix under the same filename as VST-normalized data (`vst_normalized_matrix.rds`) — downstream scripts do not distinguish between normalization strategies; GSVA `kcdf = "Gaussian"` is used regardless of mode, which is appropriate for VST but may not be optimal for log2(TPM). | Open |
| W24 | 01_human_bulk_rnaseq | Duplicate gene removal by first occurrence is arbitrary and non-deterministic — if input file ordering is not fixed, different rows may be retained across runs. A more robust approach would select the gene with highest mean expression or sum across duplicates. The number of duplicates removed is not logged. | Open |
| W25 | 01_human_bulk_rnaseq | Immune deconvolution not implemented in MICA sub-pipeline — Figure 5E panels (CD8 T cells, cytotoxic lymphocytes, monocytic lineage, myeloid DCs) were planned using a method referenced as `mcp_list` (suggesting MCPcounter), but the package is never loaded and the analysis was not implemented. | Open |
| W26 | 01_human_bulk_rnaseq | Bubble plot PDF saved twice in `05_visualize.R` — original bubble plot (lines 94 and 96), FDR bubble plot (lines 122 and 124), and comparison plot (lines 141 and 143) are each written to disk twice. Minor redundancy with no effect on results, but indicates copy-paste error. | Open |
| W27 | 02_rat_snrnaseq | scType variable name typo in `03b_sctype_annotate.R` — line 85 assigns `GenesIn` (capital G) but returns `GeneIn` (no 's'), which would cause `gene_sets_prepare()` to fail. The bug does not affect actual output because the main script bypasses this function by directly constructing `gs_list` from the custom marker database. | Open |
| W28 | 02_rat_snrnaseq | `RunHarmony()` called without explicit random seed — Harmony's internal optimization may use R's global random state (set via `set.seed(12345)`), but this depends on implementation and whether intervening stochastic operations consumed random numbers. For strict reproducibility, Harmony's `seed` parameter should be explicitly set if available. | Open |
| W29 | 03_rat_wes | `applymap()` deprecated in pandas ≥2.1.0 — `03_generate_oncoplot.py` line 187 uses `applymap()` which was deprecated in pandas 2.1.0 in favor of `map()`. Raises `FutureWarning` in current pandas versions and will fail in future releases. | Open |
| W30 | 03_rat_wes | No SLURM dependency enforced between VEP and main pipeline — `00_run_vep.sbatch` and `run_analysis.sbatch` are submitted independently with no `--dependency=afterok:<jobid>` link; pipeline relies on user submitting in correct order and waiting for VEP completion. | Open |
| W31 | 03_rat_wes | Hardcoded external input path for VCF files reduces portability — `/ix1/alee/LO_LAB/Personal/Alexander_Chang/alc376/NeilRatWES/RatWES_Mutect2_VCF_Input` is duplicated in both `00_run_vep.sbatch` and `02_cosmic_signatures.py`. | Open |
| W32 | 04_human_scrnaseq | Hardcoded absolute path in `00b_preprocess_seurat.R` — line 55 hardcodes `/ix1/alee/LO_LAB/General/Public_Data/BC-Datasets/Xu_etal_Primary_Breast_Tumor_Atlas_2024`. Institution-specific; will fail on external systems. | Open |
| W33 | 05_rat_bulk_rnaseq | Validation script references hardcoded external path to Rahul's analysis directory — `05_validate_vs_rahul.R` depends on `/ix1/alee/LO_LAB/Personal/Rahul/Neil_RNAseq`; will break if reference data are relocated. Consider copying reference files into project's `data/` directory or parameterizing the path. | Open |
| W34 | 05_rat_bulk_rnaseq | No gene-length file used; true TPM cannot be computed — pipeline does not extract or use gene lengths from the GTF annotation; without gene lengths, true TPM (requiring effective gene-length normalization) cannot be computed. If PAM50 classification accuracy is critical, consider computing TPM using effective gene lengths from the GTF or featureCounts output. | Open |
| W35 | 05_rat_bulk_rnaseq | Duplicate PAM50 ortholog mapping entries — the human-to-rat mapping contains redundant entries: both `"CDCA1"` and `"NUF2"` map to rat `"Nuf2"` (CDCA1 is an alias for NUF2/KNTC2/NDC80); `"ORC6L"` and `"ORC6"` both implicitly map to `"Orc6"`. Does not cause an error but introduces ambiguity. | Open |
| W36 | 05_rat_bulk_rnaseq | PAM50 heatmaps saved as PDF only — project CLAUDE.md requires dual-format output (PDF + PNG at 300 DPI), but `pam50_heatmap.pdf` and `pam50_probabilities_heatmap.pdf` are saved as PDF only; PNG versions were not generated. | Open |
